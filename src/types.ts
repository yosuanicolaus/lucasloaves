export type Product = {
  name: string;
  price: number;
  description: string;
};

export type Job = {
  name: string;
  description: string;
};

export type ProductOrder = {
  name: string;
  price: number;
  quantity: number;
  orderPrice: number;
};
